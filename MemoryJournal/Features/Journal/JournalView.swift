//
//  JournalView.swift
//  MemoryJournal
//
//  The Journal (Home) tab. Placeholder for Phase 0 — the real screen (create
//  entry + past-entries list) is built in Phase 3.
//

import SwiftUI

struct JournalView: View {
    var body: some View {
        ScreenPlaceholder(title: "journal")
    }
}

#Preview {
    JournalView()
}
