//
//  SettingsView.swift
//  MemoryJournal
//
//  The Settings tab. Placeholder for Phase 0 — the real settings (lookback
//  window, permissions, privacy policy) are built in Phase 6.
//

import SwiftUI

struct SettingsView: View {
    var body: some View {
        ScreenPlaceholder(title: "settings")
    }
}

#Preview {
    SettingsView()
}
